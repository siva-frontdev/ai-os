[package]
name = "execution-discovery"
version.workspace = true
edition.workspace = true
description = "Execution Platform — local and external capability discovery"

[dependencies]
execution-core = { path = "../execution-core" }
ai-os-core = { path = "../../core" }
tokio = { version = "1", features = ["sync", "process"] }
thiserror = "2"
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

[dev-dependencies]
criterion = { workspace = true }
memory-core = { path = "../../crates/memory-core" }
tokio = { version = "1", features = ["rt", "macros"] }
tokio-test = "0.4"

[[bench]]
name = "discovery_bench"
harness = false
