[package]
name = "perception-normalizer"
version.workspace = true
edition.workspace = true
description = "Perception Platform — schema validation, format detection, sanitization, normalization pipeline"

[dependencies]
perception-core = { path = "../perception-core" }
ai-os-core = { path = "../../core" }
memory-core = { path = "../../crates/memory-core" }
tokio = { version = "1", features = ["sync"] }
thiserror = "2"
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
tokio-test = "0.4"
proptest = "1"
