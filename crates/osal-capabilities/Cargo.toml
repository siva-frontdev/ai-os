[package]
name = "osal-capabilities"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dev-dependencies]
criterion = { workspace = true }
serde_json = "1"

[[bench]]
name = "capabilities_bench"
harness = false

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
